import type { McpServer } from "@modelcontextprotocol/server";
import type { EosContext } from "../../eos/context.js";

/** Phase 1: palette select/fire — see PLAN.md */
export function registerPaletteTools(_server: McpServer, _ctx: EosContext): void {
  // Planned: palette_fire (ip/fp/cp/bp), preset_fire, ...
}
